// Quiz: Polymorphism 
    // 1 Which operator returns true if the left operand is an object 
    // that is an instance of the right operand class?
        // instanceof true
        typeof
        is
        isinstanceof

    // 2 True or False: In the below code, a Bird is an instance of a Dog

        // public class Animal { }
        // public class Dog extends Animal { }
        // public class Bird extends Animal { }

            TRUE
            FALSE // true

    // 3 True or False: If one class can be described as a "specific example" of another, 
    // that relationship can be described through inheritance

        TRUE // true
        FALSE

// Quiz: Data Structures

    // A singly-linked list is a uni-directional link of nodes. 
    // Each node only knows about the node in front of it.
        TRUE // true
        FALSE

    // 2 Which data structure supports the concept of node that points to the next node?

    // Hint: It can be bi-directional or uni-directional.
        Queue
        List
        Map
        Set
        LinkedList // true

    // 3 What type of ordering is a Queue?
        LIFO
        FIFO // true
        FILO
        LILO 

// Quiz: Java Collections

    // Because LinkedLists in java implement List, they have an index
        TRUE // true
        FALSE

    // 2 "Collections" is?

                // The interface that all java collections implement
        // true  // A class filled with static methods used to manipulate collections


    // 3 True or False: An array is a type of Collection in Java

        TRUE
        FALSE // true

// Java Exceptions 

    // You can have multiple catch blocks for a single try block.
        TRUE // truye
        FALSE

    // 2 Which exception will be thrown from this code?

    // int[] a = new int[0];
    // System.out.println(a[1/0]);
        ClassCastException
        ArithmeticException  // true
        ArrayIndexOutOfBoundsException
        NumberFormatException

    // 3 True or False: A finally block will execute regardless of whether 
    // an exception is caught.
        TRUE // true
        FALSE




